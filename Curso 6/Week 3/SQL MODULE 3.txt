1-select * from EMPLOYEES where salary < AVG(salary);
2-SELECT EMP_ID, salary FROM EMPLOYEES WHERE salary < (SELECT AVG(Salary) FROM EMPLOYEES);
3-select EMP_ID, SALARY, MAX(SALARY) AS MAX_SALARY  from EMPLOYEES;
4-select EMP_ID, SALARY, ( select MAX(SALARY) from EMPLOYEES ) AS MAX_SALARY from EMPLOYEES; #select E.EMP_ID, E.SALARY, MAX_SALARY from EMPLOYEES AS E, JOB_HISTORY AS J, JOBS AS S WHERE E.EMP_ID = J.EMPL_ID AND J.JOBS_ID = S.JOB_IDENT 
5-select * from ( select EMP_ID, F_NAME, L_NAME, DEP_ID from EMPLOYEES) AS EMP4ALL; #SELECT EMP_ID, F_NAME, L_NAME, JOB_ID,MANAGER_ID,DEP_ID FROM EMPLOYEES


1-select * from EMPLOYEES where JOB_ID IN (select JOB_IDENT from JOBS); #SELECT * FROM EMPLOYEES,JOBS WHERE JOB_ID = JOB_IDENT
2- select * from EMPLOYEES where JOB_ID IN (select JOB_IDENT from JOBS where JOB_TITLE= 'Jr. Designer'); #SELECT * FROM EMPLOYEES AS E, JOBS AS J WHERE E.JOB_ID = J.JOB_IDENT AND J.JOB_TITLE = "Jr. Designer" 
3-SELECT * FROM JOBS WHERE JOB_IDENT IN (SELECT JOB_ID FROM EMPLOYEES WHERE SALARY > 70000) 
4-select JOB_TITLE, MIN_SALARY,MAX_SALARY,JOB_IDENT from JOBS where JOB_IDENT IN (select JOB_ID from EMPLOYEES where YEAR(B_DATE)>1976 );
#SELECT E.F_NAME, E.L_NAME, E.EMP_ID, E.DEP_ID, J.JOB_IDENT, J.JOB_TITLE, J.MIN_SALARY, J.MAX_SALARY, E.B_DATE FROM JOBS AS J, EMPLOYEES AS E WHERE YEAR(E.B_DATE) > 1976 AND J.JOB_IDENT = E.JOB_ID 
5-